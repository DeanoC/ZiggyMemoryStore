# ZiggyMemoryStore

Shared memory and run persistence components used by ZiggySpiderweb runtime.

## Exports

- Versioned SQLite memory store (`ltm_store`)
- MemId parse/format helpers (`memid`)
- Runtime memory state manager (`memory`)
- Run metadata/event persistence (`run_store`)

## Build

- `zig build`
- `zig build test`
