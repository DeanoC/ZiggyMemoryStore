name: ci

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  build-and-test:
    uses: DeanoC/ZiggySpiderweb/.github/workflows/ziggy-shared-ci.yml@main
    with:
      install_sqlite: true
      verify_script: |
        grep -q '\.name = \.ziggy_memory_store' build.zig.zon
        grep -q '\.version = ' build.zig.zon
